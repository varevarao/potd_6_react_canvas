import React from 'react';
import { connect } from 'react-redux';
import '../styles/CanvasGrid.css';

/**
 * Renders a grid layout, based on the supplied grid config
 */
class CanvasGrid extends React.PureComponent {
    render() {

    }
}

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(CanvasGrid);